# /var/qmail is the canonical installation root for qmail across the DSMR
# stack (matches the `qmail` package itself).  Lintian's non-standard-dir
# and file-in-unusual-dir warnings are expected and harmless here.
dsmr-checkpassword-dovecot: non-standard-dir-in-var
dsmr-checkpassword-dovecot: file-in-unusual-dir
